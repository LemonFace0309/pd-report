const Home = () => {
  return (
    <div className="container">
      <h1>{`Counter: ${0}`}</h1>
      <button className="counterBtn">Increment Counter</button>
    </div>
  )
};

export default Home;
